# 资政大模型 — 项目要求（Requirements）

> 依据《立项报告》结题目标，结合当前项目实际状况（仅剩数据与 Dify 配置）编制。
> 更新日期：2026-08-06

---

## 1. 项目概述

**项目名称**：资政大模型（基于垂直领域大模型的政府智库报告生成智能体）

**结题目标**：搭建好知识库，部署大语言模型，实现政务文字及回答生成专业化、结构化。

**核心链路**：`输入增强与向量化 → 文本检索查询 → 大模型生成输出`

**技术路线（立项承诺）**：
- **DC-RAG**（基于文档聚类的检索增强生成）：知识库构建（文档向量化 → GMM 软聚类 → 类别-文档-文本块树形知识库）+ 答案生成（问题增强向量化 → 按类别/文档/文本块顺序检索 → 大模型生成）
- **Dify**：零编码搭建 Agent 与工作流

---

## 2. 现状盘点（2026-08-06）

> 因项目管理不善，原代码（爬虫、清洗、向量化、聚类、检索）已丢失，现仅剩以下资产。

### 2.1 已有资产（可复用）

| 资产 | 位置 | 内容 | 说明 |
| --- | --- | --- | --- |
| 清洗后语料 | `gov_work_reports_sz_txt_cleaned/` | 146 个 `.txt`，约 7.8MB / 283 万字符 | 国务院（2014-2025）、广东省（2014-2025）、深圳市（2014-2025）及深圳 11 区（南山、福田、罗湖、龙岗、宝安、龙华、光明、坪山、盐田、大鹏、深汕）政府工作报告 |
| Dify 应用配置 | `Rag.yml` | Dify `advanced-chat` 应用 | 工作流：`用户输入 → 知识检索 → LLM → 直接回复`；模型 `moonshot-v1-8k`（Kimi，temperature 0.7）；top_k=3；含政务公文写作专家提示词（角色设定、上行文/下行文/工作报告/政策解读、审校纠错） |
| 立项报告 | `资政大模型--大一年度项目立项报告 (1).pdf` | 项目立项依据 | 目标、技术路线、进度安排、验收标准 |

### 2.2 缺失资产（需重建）

| 缺失项 | 说明 |
| --- | --- |
| 版本管理 | 原代码无备份导致丢失，须立即建立 git 仓库并远程备份 |
| 爬虫 | 原爬虫丢失；需重建用于知识库**动态更新**（立项创新点之一） |
| 数据清洗流水线 | 数据已清洗，但清洗脚本丢失，无法复现/增量处理 |
| 文档切分与向量化 | 嵌入模型选型、文本块切分、向量化脚本均缺失 |
| 摘要生成 | 用推理大模型生成文档摘要的流程缺失 |
| GMM 软聚类 | DC-RAG 核心组件缺失 |
| 层次化知识库 | "类别-文档-文本块"树形检索知识库缺失（含检索接口） |
| DC-RAG 检索流程 | 当前 `Rag.yml` 为**简化 RAG**（top_k 直接检索），未实现按类别→文档→文本块顺序检索，需升级 |
| Dify 知识库数据集 | `Rag.yml` 引用的 dataset（`BL4Koyago...`）为平台内 ID，本地无数据集备份，需重建后重新关联 |
| 面向用户的应用 | 用户交互应用（界面、报告/摘要/问答）未完成 |

### 2.3 差距分析（现状 vs 结题目标）

| 结题目标要求 | 现状 | 差距 |
| --- | --- | --- |
| 动态抓取最新政策数据 | 仅有存量 146 篇语料 | ❌ 爬虫与更新机制需重建 |
| 语义理解与分类存储 | 仅有清洗文本 | ❌ 向量化、聚类未实现 |
| 层次化知识库（类别-文档-文本块） | 无 | ❌ 完全缺失 |
| 推理式大模型生成结构化报告 | Dify 应用骨架存在 | ⚠️ 流程为简化 RAG，提示词已具备 |
| 政务文字专业化、结构化 | 提示词已设计 | ⚠️ 需接入 DC-RAG 检索后验证 |

---

## 3. 功能需求（FR）

### FR1 数据采集与更新
- FR1.1 重建爬虫，可抓取政府公文与政策数据（支持定时任务）
- FR1.2 新数据自动入库并支持知识库增量更新

### FR2 知识库构建（DC-RAG）
- FR2.1 文档清洗与切分（可复现的清洗流水线）
- FR2.2 嵌入模型对文档与文本块向量化
- FR2.3 推理大模型为每篇文档生成摘要
- FR2.4 基于摘要与文档做 GMM 软聚类，形成类别体系
- FR2.5 构建"类别-文档-文本块"树形检索知识库
- FR2.6 知识库可导入 Dify 数据集并支持检索

### FR3 检索与生成（DC-RAG）
- FR3.1 用户输入增强（推理大模型规范化、提取潜在语义）并向量化
- FR3.2 按"类别 → 文档 → 文本块"顺序检索，返回最相关若干条
- FR3.3 检索结果（含文档元数据与文本块）作为上下文，提交推理大模型生成答案
- FR3.4 生成内容结构化（政策报告、摘要、问答），符合政务公文规范

### FR4 Agent 与应用
- FR4.1 基于 `Rag.yml` 重建/完善 Dify Agent（advanced-chat），串联"输入增强→检索→生成"
- FR4.2 保留并完善现有政务公文写作提示词（角色设定、四类公文、审校纠错）
- FR4.3 构建面向用户的应用程序（政策报告生成、摘要、情报跟踪等）

### FR5 安全与合规
- FR5.1 支持本地或政务云部署
- FR5.2 数据隐私保护与模型输出可控（敏感词、可审计）

---

## 4. 非功能需求（NFR）

| 编号 | 需求 | 要求 |
| --- | --- | --- |
| NFR1 | 可维护性 | 全部代码纳入 git 版本管理，远程备份；目录结构清晰并有 README |
| NFR2 | 可复现性 | 清洗、向量化、聚类流程可一键/脚本化复现 |
| NFR3 | 实时性 | 知识库随新政策数据持续更新，更新周期可配置 |
| NFR4 | 检索质量 | DC-RAG 检索命中率与相关性优于简化 RAG（top_k 直接检索） |
| NFR5 | 生成质量 | 输出专业化、结构化，符合《党政机关公文格式》（GB/T 9704-2012） |
| NFR6 | 性能与成本 | 模型调用成本可控（现有 moonshot-v1-8k 可继续使用） |

---

## 5. 验收标准（对照结题目标）

结题验收时需全部满足：

- [ ] 层次化知识库搭建完成：类别-文档-文本块树形结构可检索，含 ≥146 篇存量语料并可增量更新
- [ ] 大语言模型部署完成：支持本地或政务云部署，调用稳定
- [ ] DC-RAG 检索流程跑通：按类别→文档→文本块顺序检索，效果优于简化 RAG
- [ ] 政务文字生成专业化、结构化：可生成政策报告、摘要、公文草稿，符合公文规范
- [ ] Dify Agent 与面向用户的应用可用：端到端（输入→检索→生成→展示）无断点
- [ ] 代码与数据全部纳入版本管理，可完整复现构建过程

---

## 6. 约束与假设

1. 结题时间以学院通知为准，当前处于第三阶段末（2026-08），第四阶段为九月至结题
2. 模型可继续使用 Kimi（moonshot-v1-8k）或选用同类国产模型；嵌入模型建议 BGE 系列（中文政务语料适配性好）
3. 经费按立项预算执行：资料购置约 800 元、设备与计算资源约 1700 元、应急预留 500 元
